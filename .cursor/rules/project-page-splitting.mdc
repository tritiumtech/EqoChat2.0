---
description: 项目页（Project）组件拆分与复用约定
globs: frontend/src/pages/project/**/*.vue
alwaysApply: false
---

# Project 页面组件拆分（避免几千行）

## 核心目标
- 保证 `frontend/src/pages/project/*` 下的页面文件可维护、可复用。
- 避免把列表、详情、侧栏 Drawer、Modal 等全部堆在一个页面里导致几千行代码。

## 文件大小与拆分阈值
- 若单个 `.vue` 文件（包含 `<template>` + `<script setup>` + `<style>`）超过 `600` 行，必须拆分为组件。
- 模板中出现超过 `1` 个“语义块”（例如：List/Detail、Drawer、多个 Modal、统计卡片等），优先拆成子组件。

## 拆分粒度建议（推荐目录）
在 `frontend/src/pages/project/` 下按语义拆组件：

1. `components/ProjectList.vue`：项目列表页（卡片列表 + 空态 + 创建按钮）
2. `components/ProjectDetailHeader.vue`：详情页顶部（返回按钮、分享按钮、项目标题与副信息）
3. `components/ProjectStatsGrid.vue`：四宫格统计
4. `components/PendingBidAlert.vue`：pending bid update 提示块（含 approve/reject）
5. `components/ProjectDetailsCard.vue`：Project Details 卡片（当前 bid、押金状态、团队成员数、Update Bid 按钮）
6. `components/QuickActions.vue`：快捷操作（new task / invite）
7. `components/TeamMembersCard.vue`：团队成员列表（owner 标识、transfer 按钮）
8. `components/RecentActivityCard.vue`：Recent Activity 区块

Modal 与 Drawer 建议进一步拆到：
- `components/modals/CreateProjectModal.vue`
- `components/modals/UpdateBidModal.vue`
- `components/modals/TransferOwnershipModal.vue`
- `components/modals/ShareProjectModal.vue`
- `components/ProjectSidebarDrawer.vue`

## props / emits 约定
- Modal 用 `v-model:open` 或 `open` + `onClose`（统一一种风格），并通过 props 接收业务数据。
- 子组件不要直接调用全局 API；API 只在页面顶层或 composable 中完成。
- 子组件只负责展示与“发事件”（emit），例如：`onApprove`、`onReject`、`onTransferMember`、`onCopyShare`。

## 逻辑与格式化函数复用
- 所有纯格式化/映射函数（如：`formatBidK`、`formatMoney`、`statusClass`、`taskPriorityLabel`、`paymentStatusLabel`）优先抽到：
  - `frontend/src/pages/project/utils/format.ts` 或 `frontend/src/pages/project/composables/useProjectFormat.ts`
- 需要计算的派生数据（如 `pendingTaskCount`、`currentUserIsOwner`、`recentActivityList`）建议放到 composable 或页面顶层 computed 中，再传给展示组件。

## i18n 强制
- 所有用户可见文案必须走 `vue-i18n` 的 `t()`。
- 该规则不替代已有 i18n 规则：同一语义 key 必须在中英文都补齐。

## 示例：页面顶层结构（参考模板）
```vue
<template>
  <ProjectList
    v-if="selectedProjectId == null"
    :projects="projects"
    :loading="loading"
    @openProject="openProject"
    @openCreateModal="openCreateModal"
  />

  <ProjectDetailHeader
    v-else
    :project="projectDetail"
    :pendingTaskCount="pendingTaskCount"
    @back="closeProject"
    @openSidebar="openSidebar"
    @openShareModal="openShareModal"
  />

  <ProjectSidebarDrawer
    :open="showSidebar"
    :tab="sidebarTab"
    :tasks="sidebarTasks"
    :payments="sidebarPayments"
    :files="sidebarFiles"
    @close="closeSidebar"
  />

  <TransferOwnershipModal
    v-model:open="showTransferModal"
    :member="selectedTransferMember"
    @confirm="submitTransferOwnership"
    @close="closeTransferModal"
  />
</template>
```

## 禁止
- 禁止在 `project.vue` 中把多个 Modal、Drawer、列表与详情混合写在同一个 `<template>` 里。
- 禁止把 API 调用、复杂计算与大量模板并存于同一个组件文件（必须拆层）。

